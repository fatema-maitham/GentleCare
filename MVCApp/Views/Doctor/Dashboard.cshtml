@model MVCApp.ViewModels.Doctor.DoctorDashboardViewModel

@{
    ViewData["Title"] = "Doctor Dashboard";
}

<style>
    body {
        background: #eef6ff !important;
        font-family: "Inter", "Segoe UI", Arial, sans-serif !important;
    }

    .doctor-dashboard-page {
        max-width: 1120px;
        margin: 0 auto;
        padding: 0 0 45px;
        font-family: "Inter", "Segoe UI", Arial, sans-serif;
    }

    .dashboard-stats {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 16px;
        margin-bottom: 22px;
    }

    .dashboard-stat {
        position: relative;
        overflow: hidden;
        background: #ffffff;
        border: 1px solid #d9e8fb;
        border-radius: 14px;
        padding: 20px;
        box-shadow: 0 8px 20px rgba(0, 136, 255, 0.08);
        animation: cardFadeUp 0.45s ease both;
        transition: transform 0.24s ease, border-color 0.24s ease;
    }

        .dashboard-stat:nth-child(1) {
            animation-delay: 0.03s;
        }

        .dashboard-stat:nth-child(2) {
            animation-delay: 0.08s;
        }

        .dashboard-stat:nth-child(3) {
            animation-delay: 0.13s;
        }

        .dashboard-stat:nth-child(4) {
            animation-delay: 0.18s;
        }

        .dashboard-stat::before {
            content: "";
            position: absolute;
            left: 0;
            top: 0;
            width: 0;
            height: 100%;
            background: linear-gradient(90deg, rgba(0,136,255,0.08), rgba(0,136,255,0.00));
            transition: width 0.28s ease;
        }

        .dashboard-stat:hover {
            transform: translateY(-5px);
            border-color: #0088FF;
        }

            .dashboard-stat:hover::before {
                width: 100%;
            }

        .dashboard-stat > * {
            position: relative;
            z-index: 1;
        }

        .dashboard-stat span {
            display: block;
            color: #475569;
            font-size: 13px;
            font-weight: 800;
            margin-bottom: 10px;
        }

        .dashboard-stat strong {
            display: block;
            color: #0088FF;
            font-size: 34px;
            font-weight: 900;
            line-height: 1;
            margin-bottom: 8px;
        }

        .dashboard-stat p {
            margin: 0;
            color: #64748b;
            font-size: 13px;
            line-height: 1.45;
        }

    .dashboard-main-grid {
        display: grid;
        grid-template-columns: 420px 1fr;
        gap: 22px;
    }

    .dashboard-card {
        background: #ffffff;
        border: 1px solid #d9e8fb;
        border-radius: 14px;
        overflow: hidden;
        box-shadow: 0 8px 22px rgba(0, 136, 255, 0.08);
        animation: cardFadeUp 0.55s ease both;
        transition: transform 0.24s ease, border-color 0.24s ease;
    }

        .dashboard-card:hover {
            transform: translateY(-3px);
            border-color: #0088FF;
        }

    .dashboard-card-header {
        padding: 16px 18px;
        background: #f8fbff;
        border-bottom: 1px solid #d9e8fb;
    }

        .dashboard-card-header h4 {
            margin: 0;
            color: #111827;
            font-size: 19px;
            font-weight: 900;
        }

        .dashboard-card-header p {
            margin: 5px 0 0;
            color: #64748b;
            font-size: 13px;
        }

    .calendar-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 18px 18px 10px;
    }

    .calendar-nav {
        color: #0088FF;
        text-decoration: none;
        font-size: 24px;
        font-weight: 900;
        padding: 4px 9px;
        border-radius: 8px;
        transition: background-color 0.22s ease, transform 0.22s ease;
    }

        .calendar-nav:hover {
            background: #eef6ff;
            transform: translateY(-2px);
        }

    .calendar-month {
        text-align: center;
    }

        .calendar-month h3 {
            margin: 0;
            color: #111827;
            font-size: 31px;
            font-weight: 700;
            letter-spacing: 2px;
            line-height: 1.1;
        }

        .calendar-month small {
            color: #64748b;
            font-size: 13px;
            font-weight: 800;
            letter-spacing: 1px;
        }

    .calendar-weekdays,
    .calendar-grid {
        display: grid;
        grid-template-columns: repeat(7, 1fr);
        gap: 6px;
        padding: 0 18px 18px;
    }

        .calendar-weekdays div {
            text-align: center;
            color: #111827;
            font-size: 12px;
            font-weight: 900;
            padding-bottom: 6px;
        }

    .calendar-day {
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 44px;
        text-decoration: none;
        color: #111827;
        position: relative;
        font-size: 13px;
        font-weight: 800;
        border-radius: 10px;
        transition: background-color 0.22s ease, transform 0.22s ease;
    }

        .calendar-day:hover {
            background: #eef6ff;
            transform: translateY(-2px);
        }

    .calendar-day-number {
        width: 34px;
        height: 34px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 50%;
    }

    .calendar-day.other-month {
        opacity: 0.25;
    }

    .calendar-day.selected .calendar-day-number {
        background: #0088FF;
        color: white;
        animation: selectedPulse 1.5s ease-in-out infinite;
    }

    .calendar-dot {
        position: absolute;
        bottom: 5px;
        width: 5px;
        height: 5px;
        border-radius: 50%;
        background: #0088FF;
    }

    .day-panel-body {
        padding: 18px;
    }

    .appointment-day-item {
        background: #f8fbff;
        border: 1px solid #d9e8fb;
        border-left: 4px solid #0088FF;
        border-radius: 10px;
        padding: 14px;
        margin-bottom: 12px;
        animation: cardFadeUp 0.35s ease both;
        transition: transform 0.22s ease, border-color 0.22s ease, background-color 0.22s ease;
    }

        .appointment-day-item:hover {
            transform: translateX(6px);
            border-color: #0088FF;
            background: #ffffff;
        }

        .appointment-day-item:last-child {
            margin-bottom: 0;
        }

    .appointment-time {
        color: #0088FF;
        font-size: 14px;
        font-weight: 900;
        margin-bottom: 8px;
    }

    .appointment-patient {
        color: #111827;
        font-size: 16px;
        font-weight: 900;
        margin-bottom: 4px;
    }

    .appointment-ref {
        color: #64748b;
        font-size: 13px;
        margin-bottom: 9px;
    }

    .appointment-status {
        display: inline-block;
        background: #eef6ff;
        color: #0088FF;
        border-left: 4px solid #0088FF;
        padding: 6px 10px;
        font-size: 12px;
        font-weight: 900;
        margin-bottom: 10px;
    }

    .appointment-notes {
        color: #475569;
        font-size: 13px;
        line-height: 1.45;
        margin-bottom: 12px;
    }

    .appointment-details-btn {
        display: inline-block;
        text-decoration: none;
        background: #0088FF;
        color: #ffffff;
        padding: 8px 12px;
        border-radius: 8px;
        font-size: 13px;
        font-weight: 900;
        transition: transform 0.22s ease, background-color 0.22s ease;
    }

        .appointment-details-btn:hover {
            background: #0077e0;
            color: #ffffff;
            transform: translateY(-2px);
        }

    .empty-day-message {
        background: #f8fbff;
        border: 1px solid #d9e8fb;
        color: #64748b;
        border-radius: 10px;
        padding: 14px;
        font-size: 14px;
    }

    @@keyframes cardFadeUp {
        from {
            opacity: 0;
            transform: translateY(12px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @@keyframes selectedPulse {
        0%, 100% {
            box-shadow: 0 0 0 0 rgba(0,136,255,0.28);
        }

        50% {
            box-shadow: 0 0 0 7px rgba(0,136,255,0.10);
        }
    }

    @@media (max-width: 950px) {
        .dashboard-stats {
            grid-template-columns: repeat(2, 1fr);
        }

        .dashboard-main-grid {
            grid-template-columns: 1fr;
        }
    }

    @@media (max-width: 600px) {
        .dashboard-stats {
            grid-template-columns: 1fr;
        }

        .calendar-month h3 {
            font-size: 26px;
        }
    }
</style>

<div class="doctor-dashboard-page">

    <div class="dashboard-stats">
        <div class="dashboard-stat">
            <span>Selected Day Appointments</span>
            <strong>@Model.TotalAppointmentsForSelectedDate</strong>
            <p>Total appointments on selected day</p>
        </div>

        <div class="dashboard-stat">
            <span>Upcoming</span>
            <strong>@Model.UpcomingAppointmentsCount</strong>
            <p>Future active appointments</p>
        </div>

        <div class="dashboard-stat">
            <span>Unread Notifications</span>
            <strong>@Model.UnreadNotificationsCount</strong>
            <p>Require attention</p>
        </div>

        <div class="dashboard-stat">
            <span>Patients Seen</span>
            <strong>@Model.TotalPatientsSeen</strong>
            <p>Unique completed patient visits</p>
        </div>
    </div>

    <div class="dashboard-main-grid">

        <div class="dashboard-card">
            <div class="calendar-header">
                <a asp-controller="Doctor"
                   asp-action="Dashboard"
                   asp-route-selectedDate="@Model.PreviousMonthDate.ToString("yyyy-MM-dd")"
                   class="calendar-nav">‹</a>

                <div class="calendar-month">
                    <h3>@Model.CalendarMonthLabel</h3>
                    <small>@Model.CalendarYear</small>
                </div>

                <a asp-controller="Doctor"
                   asp-action="Dashboard"
                   asp-route-selectedDate="@Model.NextMonthDate.ToString("yyyy-MM-dd")"
                   class="calendar-nav">›</a>
            </div>

            <div class="calendar-weekdays">
                <div>S</div>
                <div>M</div>
                <div>T</div>
                <div>W</div>
                <div>T</div>
                <div>F</div>
                <div>S</div>
            </div>

            <div class="calendar-grid">
                @foreach (var day in Model.CalendarDays)
                {
                <a asp-controller="Doctor"
                   asp-action="Dashboard"
                   asp-route-selectedDate="@day.Date.ToString("yyyy-MM-dd")"
                   class="calendar-day @(day.IsCurrentMonth ? "" : "other-month") @(day.IsSelected ? "selected" : "")">

                    <span class="calendar-day-number">@day.DayNumber</span>

                    @if (day.HasAppointments)
                        {
                    <span class="calendar-dot"></span>
                        }
                </a>
                }
            </div>
        </div>

        <div class="dashboard-card">
            <div class="dashboard-card-header">
                <h4>Appointments for Selected Day</h4>
                <p>@Model.SelectedDate.ToString("dddd, dd MMM yyyy")</p>
            </div>

            <div class="day-panel-body">
                @if (Model.HasSelectedDayAppointments)
                {
                    foreach (var appointment in Model.SelectedDayAppointments)
                    {
                <div class="appointment-day-item">
                    <div class="appointment-time">@appointment.TimeDisplay</div>

                    <div class="appointment-patient">@appointment.PatientFullName</div>

                    <div class="appointment-ref">
                        Reference No.: @appointment.PatientReferenceNumber
                    </div>

                    <div class="appointment-status">@appointment.StatusName</div>

                    @if (!string.IsNullOrWhiteSpace(appointment.Notes))
                            {
                    <div class="appointment-notes">@appointment.Notes</div>
                            }

                    <a asp-controller="Doctor"
                       asp-action="AppointmentDetails"
                       asp-route-id="@appointment.AppointmentId"
                       class="appointment-details-btn">
                        View Details
                    </a>
                </div>
                    }
                }
                else
                {
                <div class="empty-day-message">
                    No appointments are scheduled for this day.
                </div>
                }
            </div>
        </div>

    </div>

</div>