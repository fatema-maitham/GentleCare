@model MVCApp.ViewModels.Patient.PatientEditProfileViewModel

@{
    ViewData["Title"] = "Edit Profile";

    var profileImage = string.IsNullOrWhiteSpace(Model.CurrentProfilePicture)
        ? "/images/default-patient.png"
        : Model.CurrentProfilePicture;
}

<div class="patient-page">
    <div class="patient-edit-profile-page">

        <div class="manager-top">
            <a asp-controller="Patient"
               asp-action="Profile"
               class="btn-light">
                Back to Profile
            </a>
        </div>

        <div class="manager-card">
            <div class="card-head">
                <div>
                    <h2>Edit Profile</h2>
                    <span class="muted">Update your profile photo and contact information.</span>
                </div>
            </div>

            <form asp-controller="Patient"
                  asp-action="EditProfile"
                  method="post"
                  enctype="multipart/form-data"
                  class="patient-edit-form">

                @Html.AntiForgeryToken()

                <div asp-validation-summary="ModelOnly" class="validation-summary-errors"></div>

                <div class="patient-photo-section">
                    <div class="patient-avatar-upload">
                        <img src="@profileImage"
                             id="patientImagePreview"
                             class="patient-edit-avatar"
                             alt="Patient profile image" />

                        <label for="ProfileImageFile" class="patient-upload-icon" title="Change picture">
                            📷
                        </label>

                        <input asp-for="ProfileImageFile"
                               id="ProfileImageFile"
                               type="file"
                               accept=".jpg,.jpeg,.png,.webp"
                               class="d-none" />
                    </div>

                    <div class="patient-profile-basic-info">
                        <h3>@Model.FullName</h3>
                        <p>@Model.Email</p>
                    </div>
                </div>

                <div class="patient-edit-grid">
                    <div class="patient-edit-row">
                        <label asp-for="FullName"></label>
                        <input asp-for="FullName" />
                        <span asp-validation-for="FullName" class="text-danger"></span>
                    </div>

                    <div class="patient-edit-row">
                        <label asp-for="BloodType"></label>
                        <input asp-for="BloodType" placeholder="Example: O+" />
                        <span asp-validation-for="BloodType" class="text-danger"></span>
                    </div>

                    <div class="patient-edit-row readonly-field">
                        <label asp-for="Email"></label>
                        <input asp-for="Email" readonly />
                        <span asp-validation-for="Email" class="text-danger"></span>
                    </div>

                    <div class="patient-edit-row readonly-field">
                        <label asp-for="CPRNumber"></label>
                        <input asp-for="CPRNumber" readonly />
                    </div>

                    <div class="patient-edit-row readonly-field">
                        <label asp-for="DateOfBirth"></label>
                        <input asp-for="DateOfBirth" type="date" readonly />
                    </div>

                    <div class="patient-edit-row readonly-field">
                        <label asp-for="ReferenceNumber"></label>
                        <input asp-for="ReferenceNumber" readonly />
                    </div>

                    <div class="patient-edit-row wide">
                        <label asp-for="Address"></label>
                        <input asp-for="Address" placeholder="Enter your address" />
                        <span asp-validation-for="Address" class="text-danger"></span>
                    </div>

                    <div class="patient-edit-row">
                        <label asp-for="EmergencyContactName"></label>
                        <input asp-for="EmergencyContactName" placeholder="Emergency contact name" />
                        <span asp-validation-for="EmergencyContactName" class="text-danger"></span>
                    </div>

                    <div class="patient-edit-row">
                        <label asp-for="EmergencyContactPhone"></label>
                        <input asp-for="EmergencyContactPhone" placeholder="Emergency contact phone" />
                        <span asp-validation-for="EmergencyContactPhone" class="text-danger"></span>
                    </div>
                </div>

                <div class="patient-edit-actions">
                    <a asp-controller="Patient"
                       asp-action="Profile"
                       class="btn-light">
                        Cancel
                    </a>

                    <button type="submit" class="btn-main">
                        Save Changes
                    </button>
                </div>

            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />

    <script>
        const fileInput = document.getElementById("ProfileImageFile");
        const preview = document.getElementById("patientImagePreview");

        if (fileInput && preview) {
            fileInput.addEventListener("change", function () {
                const file = this.files && this.files[0];

                if (!file) return;

                const reader = new FileReader();

                reader.onload = function (e) {
                    preview.src = e.target.result;
                };

                reader.readAsDataURL(file);
            });
        }
    </script>
}